{
  "name": "lawn-pawn",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "start": "node backend/src/index.js",
    "build": "npm run build -w frontend",
    "heroku-postbuild": "cd frontend && npm install --production=false && npm run build -w frontend && cd ../backend && npm install"
  },
  "engines": {
    "node": "22.x",
    "npm": "10.x"
  }
}
