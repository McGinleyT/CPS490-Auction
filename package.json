{
  "name": "root",
  "private": true,
  "scripts": {
    "start": "node backend/src/index.js",
    "heroku-postbuild": "cd frontend && npm install --production=false && npm run build && cd ../backend && npm install"
  },
  "engines": {
    "node": "22.x",
    "npm": "10.x"
  }
}
